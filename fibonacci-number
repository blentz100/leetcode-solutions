/**
 * @param {number} n
 * @return {number}
 */
var fib = function(n) {
    let fiboArr = [0,1]
    for(let i = 2; i <= n; i++){
        fiboArr.push(fiboArr[i-1] + fiboArr[i-2])
    }
    return(fiboArr[n])
};
